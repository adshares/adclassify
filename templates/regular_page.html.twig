{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('css/app') }}
    {{ encore_entry_link_tags('css/regular') }}
{% endblock %}

{% block body_id %}regular-page{% endblock %}
{% block body %}
    {% block navbar %}
        <div class="container">
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="#">Adshares classify</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse"
                        data-target="#navbarSupportedContent">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('classification_index') }}">Panel</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('classification_status') }}">Status</a>
                        </li>
{#                        <li class="nav-item dropdown">#}
{#                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"#}
{#                               data-toggle="dropdown">#}
{#                                Admin#}
{#                            </a>#}
{#                            <div class="dropdown-menu">#}
{#                                <a class="dropdown-item" href="#">Add user</a>#}
{#                                <a class="dropdown-item" href="#">Users</a>#}
{#                            </div>#}
{#                        </li>#}
                    </ul>
                    <nav class="navbar-nav p-0">
                        <form class="form-inline my-2 my-lg-0" id="searchForm" action="{{ path('classification_search') }}">
                            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" name="query" value="{{ query|default('') }}">
                            <button class="btn btn-outline-primary my-2 my-sm-0" type="submit"><i class="fas fa-search mr-1"></i></button>
                        </form>
                    </nav>
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                               data-toggle="dropdown">
                                {{ app.user.fullName }} ({{ app.user.email }})
                            </a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('security_account') }}">Account</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path('security_logout') }}">Logout</a>
                            </div>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    {% endblock %}
    {% block content %}{% endblock %}
{% endblock %}
